target-version = "py39"
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F", "I", "B", "C4", "UP"]

[lint.per-file-ignores]
"docker/fetch_release.py" = ["E402"] # Shared helper in repo and Docker layouts.
